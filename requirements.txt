# ── Core ASR ──────────────────────────────────────────────────────────────────
# Không dùng RealtimeSTT (PyAudio) hay webrtcvad (pkg_resources). Dùng sounddevice + faster-whisper + energy VAD.
faster-whisper>=1.0.3
sounddevice>=0.4.7
numpy>=1.26.0

# ── Speaker diarization (optional but recommended) ────────────────────────────
# Uncomment to enable resemblyzer-based speaker tracking:
# resemblyzer>=0.1.4

# ── Translation (optional but recommended) ────────────────────────────────────
# Offline translation via CTranslate2-backed models:
# argostranslate>=1.9.6

# ── Web UI ────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
